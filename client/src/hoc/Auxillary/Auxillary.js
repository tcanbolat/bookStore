// basically a function that returns a jsx object.
// used when I dont want to create an extra parent div element to wrap my jsx elements around.
const aux = (props) => props.children;

export default aux